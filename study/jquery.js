$("#id").html("myText"); // міняємо зміст елементу

$("p").hide(); // вибираємо всі елементи тега "p"
$(".class").hide();

// селектори
$("div.menu") // всі діви класа меню
$("p:first") // перший елемент класа p
$("h1, p") // два елемента через кому
$("div p") // всі p які находяться в div
$("*") // всі елементи